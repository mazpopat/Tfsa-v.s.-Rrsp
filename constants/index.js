const { tableConfig } = require('./tableConfig');
const { taxRateBrackets } = require('./taxRateBrackets');
const { formatter } = require('./currencyFormatter');
const { lineGraphConfig } = require('./lineGraphConfig');

module.exports = {
  tableConfig,
  taxRateBrackets,
  formatter,
  lineGraphConfig,
};
